<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :topic, style: "display: block" %>
    <%= form.text_field :label, id:"label", class:"form-control"%>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content, id:"content", class:"form-control", rows:"15"%>
  </div>

  <div>
    <%= form.submit "Submit Post", class:"btn btn-outline-success w-100" %>
  </div>
<% end %>
