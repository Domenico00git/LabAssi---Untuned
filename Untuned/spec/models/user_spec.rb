require 'rails_helper'

#RSpec.describe User, type: :model do

  #it "can create a new post" do
    #user = User.create(username:"example_user", email:"user@example.com", password:"password", name:"example_name", lastname:"example_lastname", birthdate:"example_birthdate")
    #post = user.posts.create(label:"example_label", content:"example_content")

    #expect(post).to be_valid
    #expect(post.label).to eq("example_label")
    #expect(post.content).to eq("example_content")
    #expect(post.user_id).to eq(user)
  
    #pending "add some examples to (or delete) #{__FILE__}"
  #end
#end
